#include "main.h"

/**
 * mul - multiply two integers
 * @a: first parameter to be multiplied
 * @b: second parameter to be multiplied with the first
 * Return: the result of the answer
 */

int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
